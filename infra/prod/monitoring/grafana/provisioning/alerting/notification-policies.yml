# 전부 한 곳으로 보낸다. 6개짜리 알람에 라우팅 트리를 만들 이유가 없다.

apiVersion: 1

policies:
  - orgId: 1
    receiver: discord-infra
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    # 같은 알람을 4시간마다 다시 알린다. 짧으면 알람 피로, 길면 잊는다.
    repeat_interval: 4h
