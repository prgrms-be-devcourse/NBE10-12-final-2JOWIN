# 개발 워크플로우 v1.2

> 🧭 [문서 지도](README.md) · ← [12 프론트엔드 계획](12-frontend-plan.md) · [14 기술 스택](14-tech-stack.md) →

> v1.2 (2026-09-09) — §3에 **클래스 레벨 `@Transactional(readOnly = true)` 서비스의 쓰기 메서드 규약** 신설 + ArchUnit 도입(#184, #154 후속)

> v1.1 (2026-08-27) — 차수 ↔ 주차 매핑 명시(§5) · PR 셀프 체크리스트에 프론트 2줄 추가(§2)

> 브랜치 전략 · PR 파이프라인 · CI · 칸반 연동의 팀 규약. **업무 분담 v2.0(담당·Flyway 번호대)과 정합.**
> 정리 노트: 원문에 브랜치 표기 3종(`feature/인증_N` · `feat/identity_#26` · 도메인 코드)이 혼재해 **`{타입}/{도메인코드}_{이슈번호}` 하나로 통일**했고, 담당자-도메인 매핑 오류 2건을 업무 분담 v2.0 기준으로 정정했다 (15-cleanup-report.md §2-6).

## 0. 공통

### 0.1 브랜치

| 브랜치 | 용도 | 직접 push |
| --- | --- | --- |
| `main` | **최종 제출본이자 운영** — 제출 이후 배포는 여기서 나간다 | 금지 |
| `develop` | 통합 브랜치 · **개발 기간 운영** | 금지 (PR만) |
| `{타입}/{도메인코드}_{이슈번호}` | 도메인별 개발 (작업 브랜치) | 허용 |

### 0.2 네이밍

| 대상 | 규칙 | 예 |
| --- | --- | --- |
| 작업 브랜치 | `{타입}/{도메인코드}_{이슈번호}` | `feat/AU_26` · `fix/QT_41` |
| 커밋 | `{타입}: 요약` (콜론 앞 공백 없음) | `feat: refresh 토큰 회전 구현` |
| PR 제목 | `[도메인] 요약 (#이슈번호)` | `[A] 로그아웃 멱등 처리 (#24)` |
| Flyway | `V{도메인번호대}__요약.sql` | `V101__add_refresh_token_index.sql` |

- 타입(브랜치·커밋 공용): `feat` · `fix` · `refactor` · `docs` · `test` · `chore`
- 브랜치명에 `#`을 넣지 않는다 (`feat/AU_26` ⭕ / `feat/AU_#26` ✕ — 셸에서 주석으로 해석되어 사고 유발).
- Flyway 번호대: **A=1xx · B=2xx · C=3xx · D=4xx** (병렬 작업 시 버전 충돌 방지). `V1__baseline.sql`(25테이블)은 E가 작성 — 업무 분담 §1.2. 이후 각 도메인의 스키마 변경만 자기 번호대에 추가한다.

### 0.3 도메인 코드 (요구사항 정의서 2절과 동일)

| 코드 | 영역 | 코드 | 영역 |
| --- | --- | --- | --- |
| ON | 기업 온보딩 | QT | 견적 |
| MB | 구성원 관리 | AP | 고객 승인 |
| AU | 인증 | OD | 주문 |
| CU | 고객사 | AC | 활동 이력 |
| DL | Deal | SC | 조회 범위 |
| PR | 상품 카탈로그 | DB | 현황 파악 |
| | | NT | 알림 |

> ⚠️ 도메인 코드 `PR`(상품 카탈로그)·`DB`(현황 파악)는 Pull Request·데이터베이스와 표기가 겹친다 — 브랜치명·라벨에서만 쓰고 문장에서는 풀어 쓴다.

### 0.4 담당자 × 도메인 (업무 분담 v2.0 기준)

| 담당 | 이름 | 도메인 코드 |
| --- | --- | --- |
| A | 조민석 | ON · MB · AU · SC(공통 기반) |
| B | 한상민 | CU · PR · AC |
| C | 최선진 | DL · QT · OD |
| D | 이준형 | AP · NT · DB |
| E | 김대연 | 인프라 · 공통 골격 (도메인 코드 없음 — `chore/*` · CI · Flyway baseline) |

---

## 1. 브랜치 흐름

```
━━━━━━━━━━━━━ 개발 기간 ━━━━━━━━━━━━━

  feat/AU_{이슈번호} ─┐
  fix/QT_{이슈번호}  ─┼─ PR ─→ develop ─→ EC2 (운영)
  docs/DL_{이슈번호} ─┘   Squash        └─→ Release  v0.x.y-develop.N

━━━━━━━━━━━━━ 제출 시점 · 1회 ━━━━━━━━━

  develop ─ PR ─→ main ─→ EC2 (운영)
         Merge Commit  └─→ Release  v1.0.0

         develop 배포는 여기서 멈춘다
```

| 규칙 | 내용 |
| --- | --- |
| 차수 완료 판정 | 해당 차수 칸반 이슈 전부 Done + CI 통과 |
| main 머지 | develop → main 단방향. main에서 develop으로 되돌리지 않음 |
| **머지 방식** | feature → develop 은 **Squash**, develop → main 은 **Merge Commit** (§1.1) |
| **배포 출처** | 개발 기간 `develop`, 제출 이후 `main`. **동시에 둘 다 배포하지 않는다** |

> `release/{버전}` 브랜치 단계는 제거했다. EC2 가 한 대라 브랜치별 환경 분리가 성립하지 않고,
> 차수가 1차 하나뿐이라 안정화 브랜치를 한 번만 쓰게 된다 — 치르는 절차 대비 얻는 것이 적었다.

## 1.1 제출 절차

| # | 할 일 |
| --- | --- |
| 1 | 1차 이슈 전부 Done 확인 |
| 2 | `develop` → `main` PR — **`Create a merge commit` 선택** |
| 3 | 배포 트리거를 `main` 으로 바꾸는 PR (`backend-cd.yml` · `infra-cd.yml`) |
| 4 | 환경 브랜치 정책에 `main` 추가 (`backend-deploy` · `infra-apply`) |
| 5 | `main` 에서 최종 배포 → `v1.0.0` 태그 |

**함정 3개**

| | |
| --- | --- |
| **squash 금지** | 2단계에서 기본값이 `Squash and merge` 일 수 있다. 누르면 develop 의 커밋 전부가 커밋 1개로 뭉쳐 **릴리스 노트가 한 줄이 되고 팀원들의 author 기록이 사라진다.** 되돌리려면 `main` 보호 규칙을 풀어야 한다 |
| **환경 정책** | 4단계를 빠뜨리면 3단계를 해도 배포가 환경 게이트에서 막힌다 (`develop` 전용으로 잠겨 있다) |
| 새 SHA | 머지 커밋은 새 SHA라 같은 코드로 이미지가 다시 빌드된다. 낭비지만 무해하다 |

---

## 2. PR 파이프라인

```
push → PR 생성 → CI → 팀원 승인 1인 → Squash merge
```

| 단계 | 차단 여부 | 비고 |
| --- | --- | --- |
| CI (빌드·테스트·Flyway) | **차단** | required check |
| 팀원 승인 | **차단** | 1인 이상. CODEOWNERS 자동 배정 · 리뷰 관점은 §2의 셀프 체크리스트와 동일 |

### 브랜치 보호 규칙 (`main` · `develop`)

- 직접 push 금지 · force push 금지 · 브랜치 삭제 금지
- required check = CI 잡만
- 승인 1인 이상

### PR 템플릿 (`.github/PULL_REQUEST_TEMPLATE.md`에 그대로 반영)

PR 본문은 아래 양식을 따른다. 개요·관련 이슈·셀프 체크리스트는 필수, 나머지는 해당 없으면 "없음"으로 남긴다.

```markdown
## 개요
<!-- 무엇을 왜 바꿨는지 1~3줄. 리뷰어가 diff를 열기 전에 맥락을 잡을 수 있게 -->

## 관련 이슈
Closes #

## 변경 사항
<!-- 주요 변경을 항목으로. 엔드포인트/도메인 규칙 변경은 근거 문서 절을 함께 표기 -->
- 예) POST /api/v1/quotes/{id}/send 발송 시 Deal 자동 승급 구현 (07-api-spec.md §C, Q-25)

## 스키마 변경
<!-- 없으면 "없음". 있으면 마이그레이션 파일명 + ERD 버전 업 합의 여부 -->
- 없음

## 테스트
<!-- 무엇을 어떻게 검증했는지. 동시성·상태 전이가 걸린 변경은 해당 테스트를 명시 -->
- [ ] 단위 테스트 추가·갱신
- 검증 방법:

## 스크린샷 (해당 시)
<!-- API 응답 예시, 화면 변경 등 -->

## 셀프 체크리스트
- [ ] API 명세서(07-api-spec.md)에 없는 엔드포인트·필드를 추가하지 않았다 — 필요하면 문서 PR 먼저
- [ ] 새 에러는 ErrorCode enum에 있고 HTTP 상태·문구가 명세 부록과 일치한다
- [ ] 상태 전이는 전이표(05-state-transitions.md)에 있는 것만 허용했다
- [ ] 404 응답은 SC-09 통일 문구를 따른다 (존재·권한 구별 금지)
- [ ] 모든 조회에 회사(company_id) 스코프가 걸려 있다 (SC-01)
- [ ] 타인 소유 테이블을 직접 조회·매핑하지 않았다 (인터페이스 경유 — 11-work-breakdown.md §7)
- [ ] 스키마 변경 시: ERD 버전 업 합의 + 내 Flyway 번호대 사용
- [ ] (프론트) 색·간격을 Radix 토큰으로만 썼다 — 임의 hex 없음 (10-screen-design.md §2.2~2.3)
- [ ] (프론트) 에러 문구를 API 명세서 부록 상수에서 가져왔다 · API 호출은 `domains/{도메인}/api.ts` 안에서만

## 리뷰어에게
<!-- 중점적으로 봐줬으면 하는 지점, 고민되는 선택지 -->
```

---

## 3. CI

| 잡 | 내용 | 실패 시 |
| --- | --- | --- |
| build | 컴파일 + 단위 테스트 | 머지 차단 |
| flyway-validate | 적용된 마이그레이션 파일 변경(체크섬) 검사 | 머지 차단 |
| flyway-version-check | 마이그레이션 버전 번호 중복 검사 | 머지 차단 |

- 트리거: `pull_request` (develop · main 대상)
- DB는 서비스 컨테이너 Postgres에 **Flyway를 실제로 태워** 검증. `ddl-auto`는 기준으로 쓰지 않는다
- 목표 실행 시간 5분 이내

### 3.1 구조 규약 (ArchUnit)

컴파일도 런타임도 잡지 못하는 규약은 `build` 잡의 테스트로 고정한다. 규칙은
`backend/src/test/java/com/twojo/global/`에 둔다.

**클래스 레벨 `@Transactional(readOnly = true)`를 쓰는 서비스의 쓰기 메서드는 메서드 레벨
`@Transactional`로 쓰기 트랜잭션을 열어야 한다.** 빠뜨리면 읽기 전용 트랜잭션에서 돌아 Hibernate가
flush를 건너뛰고 **변경이 예외도 로그도 없이 버려진다** — 컴파일·테스트·런타임 어디서도 티가 나지 않는다.

- 판정은 **메서드 이름**으로 한다 (`create`·`update`·`send`·`mark`… — 목록은 테스트의
  `WRITE_PREFIXES`가 정본). 리포지터리 `save` 호출로 판정하면 엔티티를 고치기만 하는
  더티 체킹 쓰기(`deal.markWon()`)가 통째로 빠진다
- **새 쓰기 동사를 쓰면 `WRITE_PREFIXES`에 추가한다.** 추가를 잊으면 목록 최신화 테스트가 먼저
  실패해 알려준다 — 어노테이션을 붙인 메서드의 이름은 전부 목록에 있어야 한다
- **어노테이션이 붙었는지만으로는 안 된다.** `@Transactional(readOnly = true)`를 복사해 붙이면
  어노테이션은 있는데 여전히 읽기 전용이라 결과가 같다. 규칙도 `readOnly = false`까지 확인한다
- 클래스 레벨 `readOnly = true` 자체는 금지하지 않는다. 조회 서비스에서는 유용하다

---

## 4. Discord 알림

| 채널 | 이벤트 | 구현 |
| --- | --- | --- |
| `#git-log` | push | GitHub 기본 웹훅 (`웹훅URL/github`) |
| `#git-review` | PR 생성 · 머지 | GitHub 기본 웹훅 |

푸시 알림이 리뷰 알림을 덮지 않도록 채널을 분리한다.

---

## 5. 칸반 연동

```
이슈 생성(Todo) → 이슈 번호로 브랜치 생성(In Progress) → PR 생성(In Review) → 본문 Closes #N → 머지 시 자동 Done
```

| 컬럼 | 진입 조건 |
| --- | --- |
| Todo | 이슈 등록 |
| In Progress | 작업 브랜치 생성 |
| In Review | PR 생성 |
| Done | develop 머지 (`Closes #N` 자동) |

이슈 라벨: `A` · `B` · `C` · `D` · `E`(인프라) — 담당 + `1차` · `2차` · `3차` — 차수

> 차수 ↔ 일정 매핑: **1차 = 업무 분담 §8의 4주 계획 전체(v1 범위)** — 주차 게이트(1주 말·2주 금·3주 금·4주 중)는 1차 안의 중간 점검이다. 2·3차는 다음 버전 범위가 확정된 뒤 정의한다. (검수 보정 2026-08-26)

---

## 다음 단계

| 순서 | 할 일 | 담당 |
| --- | --- | --- |
| 1 | PR 템플릿(§2) → `.github/PULL_REQUEST_TEMPLATE.md` 반영 | 김대연 |
| 2 | 브랜치 보호 규칙 · CI 워크플로 · CODEOWNERS · Discord 웹훅 설정 | 김대연 |
| 3 | 칸반 1차 이슈 등록 (도메인·차수 라벨) | 각 담당 |
