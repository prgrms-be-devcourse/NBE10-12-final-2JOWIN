/**
 * 활동 이력 — 상담 기록·할 일·감사 로그 (이벤트 리스너로 자동 기록 적재)
 *
 * <p>담당: B 한상민 · 요구사항: AC · 소유 테이블: activity · task · audit_log
 * <p>모듈 규칙(docs/11-work-breakdown.md §7): 타 모듈은 이 패키지 루트의 공개 인터페이스만 사용한다.
 * 내부 구현(서비스·리포지토리·엔티티)은 하위 패키지에 두어 모듈 밖 접근을 차단한다.
 */
package com.twojo.activity;
