import { Placeholder } from '../../../shared/ui/Placeholder'

/** 화면 자리 — 담당 도메인 PR에서 실제 화면으로 교체된다 (비밀번호 재설정 요청) */
export function PasswordResetRequestPage() {
  return <Placeholder title="비밀번호 재설정 요청" />
}
