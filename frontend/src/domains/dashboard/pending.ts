/**
 * 서버가 아직 세지 않는 집계 — 단계별 전환율(DB-07) 하나만 남았다 (C 후속 #216).
 *
 * `SalesStatsQueryImpl.conversions`가 빈 목록을 돌려주는데 응답만으로는 **"진짜 0%"와
 * 구분할 수 없다.** 막힌 것은 단계 이동 이력이다 — `audit_log`의 `STAGE_MOVED` 적재가
 * 선행이고(#217, B 리스너), 그게 쌓여야 전이 분모·분자를 셀 수 있다.
 *
 * 이달 성사(DB-02)·담당자별 실적(DB-06·08)은 PR #274로 실구현되어 자리표시자를 걷었다.
 * 전환율 노출 시점은 D가 따로 판단한다 — #216이 머지돼도 이 플래그는 그 판단까지 남는다.
 */
export const CONVERSIONS_PENDING = true

/** 자리표시자 자리에 0 대신 쓰는 문구 — "없다"가 아니라 "아직 안 센다"임을 말한다. */
export const PENDING_LABEL = '집계 준비 중'
