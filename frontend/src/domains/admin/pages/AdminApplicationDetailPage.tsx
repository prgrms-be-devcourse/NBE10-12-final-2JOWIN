import { Placeholder } from '../../../shared/ui/Placeholder'

/** 화면 자리 — 담당 도메인 PR에서 실제 화면으로 교체된다 (가입 신청 상세) */
export function AdminApplicationDetailPage() {
  return <Placeholder title="가입 신청 상세" />
}
